import React from "react"

const SearchInput = () => {
    return (
        <input
        type="text"
        className="input"
        placeholder="Search..."
        />
    )
}

export default SearchInput